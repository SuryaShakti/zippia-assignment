import React from "react";

const Heading = ({ title }) => {
  return <div className="text-2xl md:text-3xl uppercase font-semibold text-[#333333] tracking-[3.2px]">{title}</div>;
};

export default Heading;
